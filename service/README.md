# service

Основной сервис. Одна система, которая последовательно усложняется, —
а не набор независимых примеров.

| Этап | Состояние |
|---|---|
| HTTP на `net/http` без фреймворка, graceful shutdown | — |
| PostgreSQL: миграции, транзакции, конкурентный доступ | — |
| Нагрузочный тест и профилирование | — |
| Docker, CI, деплой | — |
| Наблюдаемость: метрики, логи, трейсы | — |
| Очередь, идемпотентность, отказоустойчивость | — |
| Kubernetes, автоскейлинг | — |

Каждый этап сопровождается замером в [`../benchmarks/`](../benchmarks/).
